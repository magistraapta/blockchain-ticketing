<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    
    <div class="container">
        <div class="row">
            <h2><%= event.title %> - Detail</h2>
        </div>
        <div class="row">
            <div class="col-md-6">
                <img src="<%= event.image %>" alt="<%= event.title %>" class="img-fluid" style="width: 300px; height: 200px;">
            </div>
            <div class="col-md-6">
                <h3>Description</h3>
                <p><%= event.description %></p>
            </div>
        </div>
    </div>

    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
</body>
</html>